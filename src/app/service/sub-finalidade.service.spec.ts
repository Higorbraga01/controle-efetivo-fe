/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SubFinalidadeService } from './sub-finalidade.service';

describe('Service: SubFinalidade', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SubFinalidadeService]
    });
  });

  it('should ...', inject([SubFinalidadeService], (service: SubFinalidadeService) => {
    expect(service).toBeTruthy();
  }));
});
